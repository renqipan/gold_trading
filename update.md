# 网站日常更新规则

1. 当用户要求“更新网站”或执行日常数据更新时，优先运行 `./scripts/update_website_daily.sh`。
2. 日常更新只刷新行情、公开 JSON 和网站构建，不修改交易策略、参数、页面样式或研究结论。
3. 自动更新必须从干净的 `main` 开始，只允许四个 `public/data/*.json` 文件被提交。
4. 如果没有新的已收盘交易日，不创建提交。
5. 如果资产身份、TLS、OHLC、时效、历史不可改写、前瞻账本、策略测试、类型检查或构建任一失败，停止发布并向用户报告。
6. 如果算法表现可能失效，只报告风险；除非用户明确要求，不得在日常更新中改策略或调参。
7. 如果页面需要改版，必须由用户明确要求，并保持桌面和手机浏览器一致可用。
8. GitHub 推送成功不代表托管平台部署成功；需要部署保证时，应另行检查托管状态。
