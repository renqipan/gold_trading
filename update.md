# 网站日常更新规则

1. 当用户要求“更新网站”或执行日常数据更新时，优先运行 `./scripts/update_website_daily.sh`。
2. 日常更新只刷新行情、公开 JSON 和网站构建，不修改交易策略、参数、页面样式或研究结论。
3. 自动更新必须从干净的 `main` 开始，只允许四个 `public/data/*.json` 文件被提交。
4. 最新交易日尚未收盘时，使用程序运行时获取的黄金价格更新当天模型快照，并在页面明确标注“盘中”。同一天允许覆盖尚未定稿的尾部记录；更早的已确认历史不可改写。
5. 前瞻账本只追加已结束交易日；盘中快照不计入不可变前瞻业绩。
6. 如果没有新的价格或模型变化，不创建提交。
7. 如果资产身份、TLS、OHLC、时效、历史保护、前瞻账本、策略测试、类型检查或构建任一失败，停止发布并向用户报告。
8. 如果算法表现可能失效，只报告风险；除非用户明确要求，不得在日常更新中改策略或调参。
9. 如果页面需要改版，必须由用户明确要求，并保持桌面和手机浏览器一致可用。
10. GitHub 推送成功不代表托管平台部署成功；需要部署保证时，应另行检查托管状态。
