# 黄金交易项目审计报告

审计日期：2026-07-13

审计基线：GitHub `main@4df7b03`

行情截至：2026-07-13 盘中快照；回测基准表仍采用 2026-07-10 确认收盘结果

## 审计结论

当前正式策略仍是：

```text
120 日长期趋势
  → 绝对 CUSUM 事件
  → 10% / 14% ATR 风险预算
  → 10 ATR 止盈、6 ATR 止损
  → 固定四特征 walk-forward HMM 退出
  → t+1 开盘严格执行
```

策略只做多或空仓，不做空、不借钱，最高 100% 黄金名义敞口。清理前后的正式回测结果完全一致：

| 指标 | 审计前 | 审计后 |
| --- | ---: | ---: |
| 毛收益 | 198.65% | 198.65% |
| 5bps 净收益 | 195.56% | 195.56% |
| 8bps 网站净收益 | 193.72% | 193.72% |
| 8bps Sharpe | 1.7071 | 1.7071 |
| 最大回撤 | -10.88% | -10.88% |
| 买入 / 卖出次数 | 11 / 11 | 11 / 11 |

因此本轮属于实现清理、发布安全修复和网站增强，不是策略参数优化，也没有利用开发历史重新选参。

## 2026-07-13 全项目复审结论

本轮重新检查了数据获取、特征、HMM、信号、仓位、严格执行、回测、前瞻账本、网站 JSON、每日 shell 与发布门。核心交易逻辑合理且彼此一致：

| 模块 | 复审结果 |
| --- | --- |
| 数据输入 | 正式特征只依赖黄金 OHLC；现金收益率只进入现金计息。此前十余个宏观/ETF/持仓源没有消费者，现已移出生产链。 |
| 防前视 | HMM 使用 expanding walk-forward；冻结训练/验证边界；交易信号在 `t` 日形成并在 `t+1` 开盘执行。 |
| 仓位约束 | `long_only=true`，仓位和杠杆上限均为 1.0；现金和黄金单位不得为负。 |
| 成交模拟 | 开盘成交、盘中 ATR 障碍、隔夜跳空和双边成本均有独立测试；最后一个信号保持 pending，不假设当日收盘成交。 |
| 盘中快照 | 当天最新价进入模型与网站回测尾部，但不进入不可变前瞻账本；同日尾部可更新，确认历史不可改。 |
| 发布安全 | 原子 JSON、资产身份、时效、历史追加、策略指纹、远端竞态和工作树范围均有发布门。 |

未发现需要改变当前正式参数或推翻回测结果的逻辑错误。此次修改只收缩数据依赖、删除死抓取代码和改善运行输出，不重新选参。

依赖审查发现 Next.js 16.2.9 间接解析到存在中等严重度 XSS 公告的 PostCSS 8.4.31。项目已升级到 Next.js 16.2.10，并用 npm `overrides` 固定 PostCSS 8.5.19；`npm audit --omit=dev` 现为 0 个漏洞。未采用会错误降级到 Next 9 的 `npm audit fix --force`。

## 数据源复审与清理

原日更会依次尝试 Eastmoney、Yahoo、Stooq、FRED 多序列、CFTC、GPR、Cboe、AkShare 等端点。探测显示多个端点在当前环境持续出现 SSL EOF、403、证书链或长超时；同时正式 HMM 已固定为 `ret_20 / vol_20 / sma_gap_60 / drawdown_120`，这些辅助数据并未进入模型。

现生产链只保留：

1. `101.QO00Y` 历史缓存 + AkShare/Sina COMEX `GC` 日常延伸，用于黄金 OHLC。
2. FRED `DGS3MO`，用于空闲现金计息；缓存超过 7 天才刷新，超过 10 天阻止发布。

已从生产代码、质量报告、公开说明与源探测删除 DXY、US10Y、GLD、VIX/VIXY、CPI、COT、CFTC managed money、宏观 surprise、GPR、FOMC、MOVE、Fed funds futures 等无消费者路径，同时删除 17 个失去调用者的抓取/HTTP辅助函数。日更不再为可恢复的缓存使用打印重复 `[warn]`；真实关键失败仍会终止发布并显示失败日志。

## 已删除的代码

### XGBoost

主文件此前仍残留一条完整但不可达的旧分类模型链，而且 `XGBClassifier` 已没有导入；一旦误调用会直接报错。现已删除：

- 模型配置、阈值和重训字段。
- 大规模分类特征筛选。
- triple-barrier 标签和 purge。
- 模型拟合、walk-forward 概率、AUC/Brier/precision/recall 报告。
- 模型入场门槛、概率字段和模型退出分支。
- 特征重要性分组和网站模型字段兼容代码。

`research/requirements.txt` 原本已不含 XGBoost，因此没有依赖需要继续保留。

### 未采纳策略

现已删除每日流水线中的旧研究路径：

- 旧收盘成交回测器。
- 低于均线、低点反转、240 日新低、TSMOM 与趋势并集信号。
- 正向/负向 CUSUM、趋势退出及其他未采纳开关。
- 参数候选、入场模式比较和扩展消融。
- `scripts/evaluate_strategy_candidates.py` 及 npm 命令。
- 公开 JSON 中的 `entryModeComparison` 和旧收盘回测指标。

正式流水线现在只构造实际使用的 SMA5/20/60/120、ret20/120、vol20、ATR、sma_gap60 和 drawdown120 等字段。

## 发布安全问题与修复

### 1. TLS 校验曾被关闭

`requests verify=False`、curl `-k` 以及全局 warning 屏蔽会允许无法验证的行情进入网站。现已恢复系统 CA 证书验证并保留运行警告。

### 2. 错资产回退

COMEX 主行情与缓存均失败时，旧代码可能改用上金所 Au99.99。两者币种、价格单位和交易时段不同，会污染信号和回测。该回退已删除；现在只接受 `101.QO00Y` 或同一 COMEX 黄金序列的缓存/GC 延伸，否则发布失败。

质量报告新增 `assetIdentity`，网站检查要求其通过。

### 3. 公开 JSON 非原子写入

异常中断可能留下半写 JSON。四个公开文件现通过临时文件序列化成功后再原子替换。

### 4. 配置指纹没有覆盖实现

旧前瞻指纹只包含版本号和配置；如果忘记提升版本，函数逻辑变化可能不被发现。新指纹额外包含正式特征、HMM、信号和严格执行函数的源码 SHA-256。

当前前瞻账本仍为 0 天，因此允许一次空账本指纹迁移；一旦已有记录，指纹变化仍会强制声明新版本。

### 5. 日常发布缺少安全入口

新增 `scripts/update_website_daily.sh`：

- 只允许干净的 `main`。
- 使用 Git 目录锁防止重复执行。
- 更新前后核对远端。
- 允许发布程序运行时取得的当天 COMEX 最新价格，并在 JSON 和页面标记为盘中快照。
- 运行数据更新和完整 `npm run verify`。
- 只允许四个公开 JSON 被自动提交。
- 无有效变化时不创建空提交。
- 禁止 force push；远端在运行期间变化时停止。
- 正常只显示同步、更新、验证、发布四步摘要；某步失败时才展开最近日志。

同一天的盘中价格、信号和回测尾部可以在后续运行中安全覆盖；更早的确认历史仍不可改写。COMEX 日线按北京时间次日 08:00 后视为已结束，盘中日线不写入 append-only 前瞻账本，待结束后才固化。

## 网站检查

“价格走势 / 黄金近 360 个交易日”现增加 120 日移动平均线：

- 使用公开数据中已由完整历史计算的 `sma_120`。
- 360 日可见窗口前仍有 540 个数据点，窗口首日不存在短窗平均问题。
- MA120 纳入纵轴范围，使用独立紫色点划线和图例。

## 仍需明确的限制

1. 当前主标的是连续黄金价格 proxy，不是逐合约 GC/MGC 交易账本；未包含真实换月价差、整数合约和保证金规则。
2. 2023-02-28 之后已被反复研究，只能视为开发历史。真正前瞻记录从 2026-07-13 开始；2026-07-13 当前仍是盘中快照，因此前瞻账本仍为 0 个已结束交易日。
3. 原始 `101.QO00Y` 行情缓存位于被忽略的 `data/raw/`；新机器可用 Sina `GC` 建立同资产族历史，但不能仅凭 Git 仓库逐字节复现当前混合历史。
4. Python 依赖仍是版本下限而非带哈希锁文件；升级依赖前必须重新运行全量回测。
5. 仓库内没有 GitHub Actions 或 Vercel 部署状态核验；脚本能确认 GitHub 推送，不能确认托管平台最终部署成功。
6. 每日自动更新最好在专用 clone/worktree 中运行；目录锁不能阻止不遵守同一锁的其他程序修改工作区。
7. 当天盘中 OHLC 尚未定稿，信号、HMM 状态和展示回测可能在收盘前变化；页面虽有明确标识，仍不能把盘中建议当作已成交指令。

## 审核后的保留边界

保留并作为正式发布路径：

- `RiskConfig`
- `build_features`
- `fit_hmm` / `fit_hmm_walk_forward`
- `primary_long_signal`
- `make_cusum_events`
- `generate_signals`
- `backtest_next_open`
- `overlay_execution_state`
- `update_forward_ledger`
- 成本压力、移动区块 bootstrap、年度回测和 HMM 稳定性报告
- 数据源测试、策略执行测试和网站数据一致性检查

本报告为量化研究和软件审计，不构成投资建议。
