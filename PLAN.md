# 黄金趋势 + HMM 交易研究计划

## 当前正式策略

本项目当前只保留一套正式研究策略：`research/gold_research_pipeline.py`。

正式策略不依赖机器学习预测：

1. HMM 将黄金市场划分为牛市、熊市、震荡、恐慌四类状态，固定使用四个黄金核心特征，并按约 252 个交易日 expanding walk-forward 重训。
2. 120 日长期技术趋势确认是否具备做多候选环境；HMM 不再作为入场硬过滤，主要负责市场状态监控和退出。
3. CUSUM 波动阈值触发候选交易事件，减少固定频率采样带来的噪声。
4. 普通趋势使用 10% 计划止损风险预算；价格高于 120 日均线且 120 日收益达到 12% 时提高到 14%。
5. 交易执行只在长期趋势与 CUSUM 候选事件满足规则时入场；买入后不设置强制持仓到期。
6. XGBoost 已退出日常流水线、Python 依赖、网站数据和正式交易算法。
7. 执行器硬性限制为只做多、非负现金、最高 100% 名义仓位和不超过 1.0x 杠杆。
8. 未投资现金使用上一可得日 FRED DGS3MO 三个月美债收益率并扣减 50bps 后计息；现金贡献与黄金择时收益分开披露。

## 数据

主交易标的：

- 黄金价格：东方财富国际期货 `101.QO00Y`，COMEX 迷你黄金连续合约 proxy。
- 当前执行器模拟 0%-100% 合成黄金价格敞口；真实 GC/MGC 分月合约、整数张数、换仓和保证金账本仍需新增可靠分月数据后单独实现。

辅助因子：

- 美元指数：东方财富 `100.UDI`。
- 美债利率：东方财富 `171.US10Y`。
- 实际利率：优先使用 FRED `DFII10` 10Y TIPS real yield；当前网络取不到时回退为 US10Y - 滞后美国 CPI 同比。
- 现金收益率：FRED `DGS3MO` 三个月美债收益率，回测只使用上一可得日值，并扣减 50bps 作为产品费率和实现偏差缓冲。
- VIX：优先使用 Cboe 官方 VIX 历史序列；`107.VIXY` 仅作为备用风险 proxy。
- ETF 资金流 proxy：`107.GLD` 量价方向签名成交额，按 60 日成交额中位数归一化。
- COT 持仓：AkShare CFTC 黄金多空与净仓位，报告日后 3 天才进入模型。
- CFTC managed money：CFTC 官方 disaggregated futures-only COT 黄金 managed money 多头、空头、净头寸和净头寸/总持仓量，报告日后 3 天才进入模型。
- CPI：AkShare 美国 CPI 同比，按下一月中旬才进入模型，降低隐性未来函数。
- 宏观 surprise：AkShare 美国 CPI MoM、核心 CPI MoM、非农就业 actual - forecast，按公布日进入模型。
- GPR：Caldara-Iacoviello 月度地缘政治风险指数，月末后 7 天才进入模型。
- FOMC：美联储官网 FOMC 会议日历，生成事件日和事件 proximity 特征。
- 暂不可用或仅作记录：MOVE index、Fed funds futures implied rate、GLD 官方日度持仓、黄金 ETF 官方净流入。流水线不会用不稳定来源伪造这些字段。
- 技术指标：收益率、动量、波动率、均线、ATR、ADX、RSI、Donchian、MACD、布林带、随机指标、收益分布、跨市场短滞后收益和趋势质量。

原始数据缓存保存在 `data/raw/`，本地日志保存在 `local_logs/`，这些目录不提交到 GitHub。

## 训练与回测流程

1. 下载或读取缓存数据，并检查 OHLC、缺失值和数据日期。
2. 构建技术、宏观、资金流、COT 与 HMM 状态特征。
3. 冻结训练截止日为 2021-02-23、验证截止日为 2023-02-27；之后的历史区间已经在多轮研究中被查看，只作为开发样本。优化版本从 2026-07-13 起积累真正 forward holdout。
4. 输出低点入场模式、参数邻域和预注册多窗口候选报告；2023-02-28 之后的开发区间禁止参与候选晋级。
5. 扩展消融继续使用统一次日开盘执行器，但只写本地研究日志，不写入网站 JSON，也不在网页展示。
6. 正式历史回测、网站状态与模拟执行统一使用 `t` 日收盘信号、`t+1` 日开盘成交、盘中 ATR 障碍、现金/持仓单位、双边交易成本、现金计息和回撤约束。
7. 网站主曲线、主收益和 Sharpe 统一使用更保守的 8bps 账本；另输出 0/5/8/15/25bps 成本压力和 20 日移动区块 bootstrap。
8. 真正 forward 记录带策略版本和配置指纹，只允许向 `public/data/gold_forward_ledger.json` 追加；历史记录变化会让流水线直接失败。任何正式策略或执行器代码变化必须同步提升 `FORMAL_STRATEGY_VERSION` 或 `EXECUTION_ENGINE_VERSION`。

## 当前交易规则

- 机器学习：正式流水线不训练分类模型，也不读取任何概率分数。
- 买入规则：120 日长期趋势 + CUSUM。低于 60/120 日均线、240 日新低、低点反转确认以及趋势与低点并集模式均已按同一严格口径测试，未被采用。
- 卖出：ATR 障碍或 HMM 趋势破坏确认。
- 单次仓位：只做多，最高 100%，杠杆上限 1.0x，现金和持仓单位必须非负；按 `风险预算 / (6 ATR / 入场价)` 缩放。普通趋势预算 10%，强趋势预算 14%，强趋势要求价格高于 120 日均线且 120 日收益至少 12%。隔夜跳空可能使实际损失超过计划值。
- 候选事件间隔：最小 5 个交易日，减少重复入场噪声。
- 止盈：10 ATR。
- 止损：6 ATR。
- HMM 退出：熊市/恐慌且价格跌破 60 日均线连续确认 20 天。
- 回撤控制：18% 软回撤把已有仓位降至原目标的 50%；30% 硬回撤清仓并冷却 63 个交易日，冷却结束后重置风险峰值。
- 现金管理：空闲现金按滞后一日三个月美债收益率扣减 50bps 后计息；Sharpe 使用相同现金收益率计算超额收益，避免把现金利息错误抬高风险调整指标。
- 实际退出：ATR 止盈、ATR 止损或 HMM 趋势破坏确认。

## 最新回测摘要

最近一次优化回测日期：2026-07-12，数据截至最近交易日 2026-07-10。

- 正式次日开盘、盘中障碍开发样本毛收益：198.65%。
- 同期买入持有收益：125.13%。
- 5bps 成本后策略净收益：195.56%；不计现金收益时为 192.06%，现金收益贡献约 3.50 个百分点。
- 网站使用 8bps 成本口径：净收益 193.72%，超额收益 Sharpe 1.71，最大回撤 -10.88%，11 次买入、11 次卖出。
- 15bps 与 25bps 压力成本下净收益分别为 189.48% 和 183.53%。
- 最大回撤：-10.88%，同期买入持有最大回撤为 -25.73%。
- 冻结验证段只有 3 次入场、2 次退出，仅 2 笔完整交易。加入同一现金政策后，正式参数收益 14.09%；12 ATR 仅 7.52%，7 ATR 止损为 14.22%，优势不到预设的 0.5 个百分点实质门槛。
- 预注册的三段 anchored walk-forward 中，12 ATR 与 7 ATR 都只有 1 个窗口达到实质收益改善，均未通过晋级门槛；不能依据已查看开发段中 12 ATR 的较高数字改参。
- 低点入场没有改善策略：低于 60 日均线模式验证收益 6.94%、开发样本收益 86.68%；低于 120 日均线模式开发样本收益 16.20%。趋势并集没有同时改善收益、Sharpe 和回撤。
- 240 日新低叠加也未改善：趋势或 240 日低点反转的验证收益仅 6.52%，开发样本没有任何独立 240 日新低交易。

正式策略选择动态风险约束后的 120 日趋势 + 绝对 CUSUM + ATR，并用固定四特征 walk-forward HMM 负责退出。旧 HMM 曾在 2025 年自动增加特征并把联合训练样本从 1,597 行骤降到 423 行；该不稳定收益已经移除。真正前瞻样本从 2026-07-13 开始，当前为 0 个交易日。

邻近 ATR、HMM 确认期、CUSUM 阈值和事件间隔只产生很少的验证交易，不足以证明参数增益，因此保留 10 ATR 止盈、6 ATR 止损、20 日 HMM 确认和 5 日事件间隔，避免根据历史测试结果追参。

## 下一步优化方向与防过拟合约束

1. **盈利后跟踪止损挑战者**：只测试一个固定版本——达到现有 10 ATR 盈利线后，不立即止盈，改为使用前一日已知最高价减 6 ATR 的跟踪止损；不搜索新倍数。止损只有在存在动量时才可能改善期望收益，因此必须作为 paper-shadow，而不是直接替换正式规则。[Kaminski & Lo](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=968338)
2. **多周期趋势只调整仓位**：保留正式入场，只用预先固定的 63/126/252 日收益方向形成 `1/3、2/3、1` 仓位置信度，并设置 no-trade band；不在当前开发段搜索周期或权重。跨资产时间序列动量提供经济先验，但不能直接证明黄金单品种有效。[Moskowitz、Ooi、Pedersen](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2089463)
3. **更长、独立的数据载体**：优先取得逐合约 GC/MGC、真实换月和 2004 年以来 GLD 调整后数据，用完全相同参数复核；连续 proxy 上不研究 carry。
4. **多重检验控制**：所有候选进入完整试验登记表，逐步加入 Deflated Sharpe Ratio 与 CSCV/PBO；当前移动区块 bootstrap 只描述路径不确定性，不是多重检验校正。[PBO](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2326253)、[DSR](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2460551)
5. **宏观变量一次只研究一个**：若 point-in-time 数据完整，优先测试 126 日实际利率方向作为 `100%/75%` 仓位修饰；不得同时堆叠美元、VIX、CPI、就业和 COT。
6. **晋级标准**：至少 2 个 anchored 窗口分别提高收益 0.5 个百分点、Sharpe 0.05；冻结验证不得落后；最大回撤最多恶化 2 个百分点，换手最多增加 25%，15bps 成本下仍需优于基线。真正 forward 数据不得回流调参。

上述结果为开发历史回测，不构成投资建议。

## 输出文件

- `public/data/gold_research_latest.json`：网站摘要、今日信号和回测指标。
- `public/data/gold_price_series.json`：网站价格、均线、状态和信号序列。
- `public/data/gold_backtest.json`：网站使用的 8bps 开发样本净值曲线。
- `public/data/gold_forward_ledger.json`：带策略版本和配置指纹的 append-only 真正前瞻记录。
- `local_logs/gold_signals.csv`：本地信号日志，包含状态、仓位、止损线和交易指南。
- `local_logs/gold_ablation.csv`：统一严格次日开盘、5bps 成本口径的扩展消融实验结果。
- `local_logs/gold_live_execution.csv`：`t+1` 开盘成交实盘模拟日志。
- `local_logs/gold_backtest_yearly.csv`：正式执行口径的分年度收益、Sharpe、回撤、仓位和换手。
- `local_logs/gold_backtest_robustness.json`：0/5/8/15/25bps 成本压力与移动区块 bootstrap。
- `local_logs/gold_candidate_pseudo_oos*.csv/json`：排除 2023 年后开发段的预注册多窗口候选筛选。
- `local_logs/gold_parameter_stability.csv`：fallback 参考参数及相邻止盈、止损、HMM 确认期和事件间隔的冻结验证/历史测试稳定性报告。
- `local_logs/gold_entry_mode_comparison.csv`：低于 60/120 日均线、低点反转确认及趋势并集模式的冻结验证/历史测试对照。
- `local_logs/gold_hmm_stability.csv`：每次 HMM 重训的固定特征版本、训练样本、收敛、状态占比、映射和转移矩阵。
- `local_logs/data_quality_report.json`：本地数据质量报告。

网站展示数字应始终由上述 `public/data/*.json` 驱动。下次更新时运行 `npm run update:site`，不要在 `app/page.tsx` 中手动改价格、收益率或仓位。
