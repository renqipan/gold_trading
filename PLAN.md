# 黄金交易项目计划

更新日期：2026-07-13。网站行情截至：2026-07-13 盘中快照；最近确认收盘日为 2026-07-10。

## 当前目标

项目只维护一套可发布策略和一套严格执行器。日常更新只能刷新数据、重建网站和追加前瞻记录，不再运行历史候选策略或自动调参。

正式策略位于 `research/gold_research_pipeline.py`：

1. 120 日长期趋势确认做多环境。
2. 绝对 CUSUM 事件触发入场。
3. 普通趋势使用 10% 计划止损风险预算；价格高于 120 日均线且 120 日收益达到 12% 时使用 14%。
4. 10 ATR 止盈、6 ATR 止损。
5. 固定四特征 `ret_20 / vol_20 / sma_gap_60 / drawdown_120` 的 expanding walk-forward HMM 负责状态识别；熊市或恐慌且跌破 60 日均线连续 20 天后退出。
6. 18% 软回撤减半仓位；30% 硬回撤清仓并冷却 63 个交易日。
7. 只做多或空仓，现金和黄金单位均不得为负，名义仓位和杠杆硬上限均为 1.0x。
8. 空闲现金使用上一可得日 FRED DGS3MO，扣减 50bps 后计息。

## 数据与发布边界

- 主标的固定为东方财富 `101.QO00Y` COMEX 迷你黄金连续合约 proxy；主源不可用时只允许同一 COMEX 黄金序列的本地缓存或 GC 延伸。
- 禁止用上金所 Au99.99 等不同币种、单位或交易时段的资产静默替代。
- 所有 HTTPS 请求必须验证 TLS 证书。
- 黄金 OHLC、现金收益率、数据陈旧度和资产身份必须通过质量门，网站才可发布。
- 四个网站 JSON 使用原子替换，避免更新中断留下半写文件。
- 网站价格图展示最近 360 个交易日的收盘价及 5/20/60/120 日均线。
- 最新 COMEX 交易日未结束时，程序运行时取得的最新价格仍进入趋势、HMM、仓位建议和网站回测快照；页面必须显示“盘中快照”和模型更新时间。
- 同一交易日的盘中尾部允许更新，更早的已确认记录不可改写；真正前瞻账本只追加已结束交易日。

## 回测口径

- 冻结训练截止日：2021-02-23。
- 冻结验证截止日：2023-02-27。
- 2023-02-28 之后已经被多轮研究查看，只能称为开发历史。
- 真正前瞻记录从 2026-07-13 开始，并由 `public/data/gold_forward_ledger.json` 按配置指纹追加。
- 信号使用 `t` 日收盘信息，交易在 `t+1` 日开盘执行；ATR 障碍按盘中价格处理，跳空穿越按开盘价处理。
- 回测逐日记录现金和黄金单位，不假设免费再平衡；网站主口径使用双边 8bps 成本。
- 保留 0/5/8/15/25bps 成本压力和移动区块 bootstrap，作为正式策略的稳健性报告。

截至 2026-07-10 确认收盘数据的开发历史结果：

- 毛收益 198.65%。
- 5bps 净收益 195.56%。
- 8bps 网站净收益 193.72%，Sharpe 1.71，最大回撤 -10.88%。
- 同期买入持有收益 125.13%，最大回撤 -25.73%。
- 11 次买入、11 次卖出。

本轮代码清理前后，上述正式策略收益、Sharpe、回撤、换手和交易次数完全一致。

## 已完成的代码清理

- 删除完整 XGBoost 配置、特征、标签、训练、验证、门槛、概率信号和模型退出死代码。
- 删除旧收盘成交回测器。
- 删除低点、TSMOM、趋势退出、参数候选、扩展消融及相关比较路径。
- 删除 `scripts/evaluate_strategy_candidates.py` 和对应 npm 命令。
- 删除公开 JSON 中的旧候选比较与旧收盘回测指标。
- 正式 HMM、严格次日开盘执行器、成本压力、bootstrap、前瞻账本和数据源测试继续保留。

## 日常更新

一键执行：

```bash
./scripts/update_website_daily.sh
```

也可运行：

```bash
npm run update:daily
```

脚本只允许在干净的 `main` 上运行，会：

1. 使用目录锁防止重复启动。
2. 快进同步 `origin/main`。
3. 获取最新数据并运行完整 `npm run verify`。
4. 只允许四个 `public/data/*.json` 发生变化。
5. 当天未收盘时发布最新盘中价格与模型快照，并允许同一天继续刷新尾部记录。
6. 前瞻账本排除盘中日线，只在交易日结束后追加。
7. 无价格或模型变化时不创建空提交。
8. 再次确认远端未变化后提交并推送 `main`。

## 下一阶段

1. 从 2026-07-13 起积累不可回写的真正前瞻记录；在获得足够交易前不调整正式参数。
2. 获取逐合约 GC/MGC、真实换月、合约乘数和整数张数数据，替换连续价格 proxy 的合成敞口假设。
3. 为每日脚本配置专用 clone 或工作目录，并在托管平台增加 main 分支保护和构建检查。
4. 若未来研究新策略，必须在独立脚本和事前登记中进行，不得进入每日生产流水线；只有通过冻结门槛后才允许替换正式实现。

## 正式输出

- `public/data/gold_research_latest.json`：网站摘要、交易指南、质量报告和回测指标。
- `public/data/gold_price_series.json`：价格、5/20/60/120 日均线、状态和执行信息。
- `public/data/gold_backtest.json`：8bps 开发历史净值。
- `public/data/gold_forward_ledger.json`：append-only 真正前瞻记录。
- `local_logs/gold_signals.csv`：本地信号日志。
- `local_logs/gold_live_execution.csv`：严格执行日志。
- `local_logs/gold_backtest_yearly.csv`：分年度回测。
- `local_logs/gold_backtest_robustness.json`：成本压力和 bootstrap。
- `local_logs/gold_hmm_stability.csv`：HMM 重训稳定性。
- `local_logs/data_quality_report.json`：数据质量报告。

本项目仅用于量化研究和历史模拟，不构成投资建议。
